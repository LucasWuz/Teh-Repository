Insert autoload stuff here

An autoload is basically a script or scene that is always active
Good for global stuff

But remember, since they're always active, you must reset its stuff
manually

_ IsItLucas?
